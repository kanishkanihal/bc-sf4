{% extends 'base.html.twig' %}

{% block title %}CmsBlock{% endblock %}

{% block body %}
    <a href="{{ path('bigcommerce-load') }}">App home</a>
    <h1>CmsBlock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cms_block.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ cms_block.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ cms_block.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cms_block_index') }}">back to list</a>

    <a href="{{ path('cms_block_edit', {'id': cms_block.id}) }}">edit</a>

    {{ include('cms_block/_delete_form.html.twig') }}
{% endblock %}
